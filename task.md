# M2L1 章节富化任务清单

- [x] 在 `hld-lld-requirements.mdx` 中新增 Mermaid 短链接跳转时序图 (图 3: Short URL Redirection Sequence)
- [x] 在 `hld-lld-requirements.mdx` 中新增 `TradeoffExplorer` 交互组件 (短链接生成策略对比)
- [x] 在 `hld-lld-requirements.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 HLD, LLD, CAP)
- [x] 在 `hld-lld-requirements.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交修改代码并输出完成报告
