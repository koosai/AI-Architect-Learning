# M5L7 章节富化任务清单

- [x] 在 `retry-backoff-timeout.mdx` 中新增 Mermaid 多级重试流量放大雪崩示意图 (图 1: Multi-layer retry amplification effect)
- [x] 在 `retry-backoff-timeout.mdx` 中新增 Mermaid 纯指数退避与加入随机抖动流量波形对比图 (图 2: Fixed delay vs Exponential backoff vs Jitter)
- [x] 在 `retry-backoff-timeout.mdx` 中新增 Mermaid 全局重试预算网关层拦截决策时序图 (图 3: Retry Budget logic in Gateway)
- [x] 在 `retry-backoff-timeout.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 Idempotency, Retry Storm)
- [x] 在 `retry-backoff-timeout.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [/] 提交修改代码并输出完成报告
