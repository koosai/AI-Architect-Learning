# M5L8 章节富化任务清单

- [x] 在 `bulkhead-isolation.mdx` 中新增 Mermaid 全共享线程池崩溃 vs 舱壁资源分舱对比图 (图 1: Shared pool thread exhaustion vs Bulkhead isolation)
- [x] 在 `bulkhead-isolation.mdx` 中新增 Mermaid 多维隔离爆炸半径全景示意图 (图 2: Multi-scale isolation architecture)
- [x] 在 `bulkhead-isolation.mdx` 中新增 Mermaid 信号量舱壁并发计数申请与释放时序图 (图 3: Semaphore Bulkhead acquire/release workflow)
- [x] 在 `bulkhead-isolation.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 Noisy Neighbor, Cell-based Architecture, Thread Pool Isolation)
- [x] 在 `bulkhead-isolation.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交修改代码并输出完成报告
