# M7L7 章节富化任务清单

- [x] 在 `kv-cache-serving.mdx` 中进行组件引入与术语包裹
- [x] 在 `kv-cache-serving.mdx` 中新增 Mermaid 朴素自回归 vs KV Cache 计算开销对比图 (图 1)
- [x] 在 `kv-cache-serving.mdx` 中新增 Mermaid vLLM PagedAttention 物理分页显存分配映射图 (图 2)
- [x] 在 `kv-cache-serving.mdx` 中新增 Mermaid 前缀缓存哈希匹配与跳过 Prefill 流程图 (图 3)
- [x] 在 `kv-cache-serving.mdx` 中新增 StepFlow 交互组件演示前缀缓存延迟优化
- [x] 在 `kv-cache-serving.mdx` 中充实「常见误解」并引入 QuizCard (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交代码并输出完成报告
