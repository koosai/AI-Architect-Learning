# M3L5 章节富化任务清单

- [x] 在 `cache-patterns.mdx` 中新增 Mermaid 四种缓存模式对比图 (图 1: Caching Patterns Architecture)
- [x] 在 `cache-patterns.mdx` 中新增 Mermaid Cache-Aside 读写时序图 (图 2: Cache-Aside Read/Write Paths)
- [x] 在 `cache-patterns.mdx` 中新增 Mermaid Write-Back 异步刷盘图 (图 3: Write-Back Asynchronous Queue)
- [x] 在 `cache-patterns.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 Write-Behind, Cache Eviction)
- [x] 在 `cache-patterns.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交修改代码并输出完成报告
