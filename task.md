# M5L5 章节富化任务清单

- [x] 在 `rate-limiting.mdx` 中新增 Mermaid 令牌桶与漏桶物理拓扑对比图 (图 1: Token Bucket vs Leaky Bucket conceptual comparison)
- [x] 在 `rate-limiting.mdx` 中新增 Mermaid 固定窗口边界双倍流量 vs 滑动窗口边界判定图 (图 2: Fixed vs Sliding Window edge cases)
- [x] 在 `rate-limiting.mdx` 中新增 Mermaid 分布式集群限流与共享 Redis 交互及降级路由图 (图 3: Distributed rate limiting with Redis)
- [x] 在 `rate-limiting.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 429, QPS)
- [x] 在 `rate-limiting.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [/] 提交修改代码并输出完成报告
