# M5L9 章节富化任务清单

- [x] 在 `replication.mdx` 中新增 Mermaid 同步复制 vs 异步复制对比时序图 (图 1: Sync vs Async replication flow)
- [x] 在 `replication.mdx` 中新增 Mermaid 读己之写不一致与高可用解决时序图 (图 2: Read-Your-Writes anomaly and mitigation)
- [x] 在 `replication.mdx` 中新增 Mermaid 法定人数 Quorum 强弱一致性对比拓扑 (图 3: Strong vs Weak Quorum comparison)
- [x] 在 `replication.mdx` 中包裹 `GlossaryTerm` 术语 (如 Read-Your-Writes)
- [x] 在 `replication.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交修改代码并输出完成报告
