# M7L3 章节富化任务清单

- [x] 在 `sampling-decoding.mdx` 中进行组件引入与术语包裹
- [x] 在 `sampling-decoding.mdx` 中新增 Mermaid 采样解码处理管道图 (图 1)
- [x] 在 `sampling-decoding.mdx` 中新增 Mermaid 温度对分布曲线影响对比图 (图 2)
- [x] 在 `sampling-decoding.mdx` 中新增 Mermaid Top-p 累计概率动态截断原理图 (图 3)
- [x] 在 `sampling-decoding.mdx` 中新增 StepFlow 交互组件演示 Logits 解码全流程
- [x] 在 `sampling-decoding.mdx` 中充实「常见误解」并引入 QuizCard (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交代码并输出完成报告
