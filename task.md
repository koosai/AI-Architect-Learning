# M5L10 章节富化任务清单

- [x] 在 `partitioning-sharding.mdx` 中新增 Mermaid 范围分片与哈希分片特征对比拓扑图 (图 1: Range vs Hash partitioning comparison)
- [x] 在 `partitioning-sharding.mdx` 中新增 Mermaid 简单哈希取模与预分片再平衡对比图 (图 2: Consistent Hashing Rebalancing vs Flat Hashing)
- [x] 在 `partitioning-sharding.mdx` 中新增 Mermaid Scatter-Gather 尾延迟广播查询图 (图 3: Scatter-Gather querying pattern and tail latency amplification)
- [x] 在 `partitioning-sharding.mdx` 中包裹 `GlossaryTerm` 术语 (如 Data Skew)
- [x] 在 `partitioning-sharding.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交修改代码并输出完成报告
