# M4L12 章节富化任务清单

- [x] 在 `parking-lot-lld.mdx` 中新增 Mermaid 停车场 LLD 类静态结构图 (图 1: ParkingLot LLD class diagram)
- [x] 在 `parking-lot-lld.mdx` 中新增 Mermaid 停车流程对象交互时序图 (图 2: Parking flow sequence diagram)
- [x] 在 `parking-lot-lld.mdx` 中新增 Mermaid LLD 设计方法论思维推导图 (图 3: LLD design methodology steps)
- [x] 在 `parking-lot-lld.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 LLD, GRASP, YAGNI)
- [x] 在 `parking-lot-lld.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [x] 提交修改代码并输出完成报告
