# M5L3 章节富化任务清单

- [x] 在 `service-discovery.mdx` 中新增 Mermaid 客户端发现与服务端发现拓扑对比图 (图 1: Client-side vs Server-side discovery comparison)
- [x] 在 `service-discovery.mdx` 中新增 Mermaid 实例心跳续约与过期自动摘除时序图 (图 2: Periodic heartbeat and eviction sequence)
- [x] 在 `service-discovery.mdx` 中新增 Mermaid 注册中心的 CAP 取舍逻辑图 (图 3: CAP trade-off in registration registry)
- [x] 在 `service-discovery.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 AP, CP, TTL)
- [x] 在 `service-discovery.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [/] 提交修改代码并输出完成报告
