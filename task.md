# M5L2 章节富化任务清单

- [x] 在 `api-gateway.mdx` 中新增 Mermaid 无网关直连 vs 有 API 网关路由拓扑对比图 (图 1: Topology comparison with/without gateway)
- [x] 在 `api-gateway.mdx` 中新增 Mermaid 网关请求管道中间件链执行时序图 (图 2: Gateway request processing middleware pipeline)
- [x] 在 `api-gateway.mdx` 中新增 Mermaid BFF (Backend for Frontend) 多端定制网关架构图 (图 3: BFF pattern)
- [x] 在 `api-gateway.mdx` 中多处包裹 `GlossaryTerm` 术语 (如 JWT, Cross-cutting Concern, SPOF)
- [x] 在 `api-gateway.mdx` 中充实「常见误解」并引入 `QuizCard` 测验 (3 道自测试题)
- [x] 运行 `npm run build` 验证构建与 Mermaid 语法正确性
- [/] 提交修改代码并输出完成报告
